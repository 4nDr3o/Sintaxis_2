#include<stdio.h>
#include<math.h>
#include<iostream>

float a,b,c;

void main() // Funcion principal
{
    a=2;
    b=8;
    c=5;
    //printf("Valor de c = ",c);
    //scanf("%f",&c);
    //printf("Valor de b= ", b);
    //printf("Valor de a", a);
    if (a==5)
    {
        printf("Valor de a", a);
        if (b==8)
            printf("El valor de b = ",b);   
    }
    
}